{
  "name": "address-book-client",
  "version": "1.0.0",
  "description": "ember and ember-cli powered client for the address-book programming exercies",
  "private": true,
  "directories": {
    "doc": "doc",
    "test": "tests"
  },
  "scripts": {
    "start": "grunt server",
    "build": "ember build",
    "test": "ember test"
  },
  "engines": {
    "node": ">= 0.10.0"
  },
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-connect-proxy": "^0.2.0",
    "grunt-contrib-connect": "^0.10.1"
  }
}
